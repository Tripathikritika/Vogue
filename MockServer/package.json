{
  "name": "mockserver",
  "version": "1.0.0",
  "description": "React Card Project using React Routings for a e-commerce website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch database.json"
  },
  "author": "Kritika Tripathi",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
